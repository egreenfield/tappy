# Copy to deploy/.env (git-ignored) and fill in.
TAPPY_HOST=musicbox.local
TAPPY_USER=pi
TAPPY_PASSWORD=changeme
# Optional. Where the code lives on the Pi. Default: /home/$TAPPY_USER/dev/tappy
#TAPPY_DIR=/home/pi/dev/tappy
