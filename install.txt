Installation and updates are scripted. See deploy/README.md:

  ./deploy/install.sh   # fresh Raspberry Pi
  ./deploy/update.sh    # push new code / apply environment changes
  ./deploy/status.sh    # what's running
